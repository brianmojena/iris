## What and why

<!-- What changed, and the reason. The reason is the part that is hard to
     recover later. -->

## How you convinced yourself it works

<!-- If this touches the render pipeline, what did you render and what did you
     measure? A number beats an adjective. -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] `npm run build` passes
- [ ] New user-facing strings are in **both** dictionaries in `src/i18n/`
- [ ] New regression tests were confirmed to fail with the bug reintroduced
